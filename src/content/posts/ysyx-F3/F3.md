---
title: 一生一芯-F3学习记录
published: 2026-03-04
tags: [OSOC, Digital circuit, Logic design]
category: OSOC
draft: false
---

# F3 数字逻辑电路基础

先放一个异或门的电路图：

![](./xor1.png)

## 通过晶体管搭建门电路

![](./MOS.png)

**nMOS晶体管的电气特性**
- 当栅极电压和源极电压之间的电压差($V_{GS}$)大于阈值电压时，晶体管导通，电流可以通过。
- 当栅极电压和源极电压之间的电压差($V_{GS}$)小于阈值电压时，晶体管截止，电流不能通过。
- 可简记为：栅极电压高于源极电压时，晶体管导通，电流可以通过。

**pMOS晶体管的电气特性**
- 当源极电压和栅极电压之间的电压差($V_{SG}$)大于阈值电压时，晶体管导通，电流可以通过。
- 当源极电压和栅极电压之间的电压差($V_{SG}$)小于阈值电压时，晶体管截止，电流不能通过。
- 可简记为：源极电压高于栅极电压时，晶体管导通，电流可以通过。

### 例：与非门
![](./nand.png)

分四种情况讨论：
| A | B | nMOS1 | nMOS2 | pMOS1 | pMOS2 | Y |
|---|---|-------|-------|-------|-------|------|
| 0 | 0 | 截止 | 截止 | 导通 | 导通 | 1 |
| 0 | 1 | 截止 | 导通 | 导通 | 截止 | 1 |
| 1 | 0 | 导通 | 截止 | 截止 | 导通 | 1 |
| 1 | 1 | 导通 | 导通 | 截止 | 截止 | 0 |

因此逻辑表达式为：
$$
Y = \overline{AB}
$$

这就是与非门。

将与非门的输出连接到非门的输入，就得到了与门。

### Ex1:
![](./exercise1.png)

分四种情况讨论：
| A | B | nMOS1 | nMOS2 | pMOS1 | pMOS2 | Y |
|---|---|-------|-------|-------|-------|------|
| 0 | 0 | 截止 | 截止 | 导通 | 导通 | 1 |
| 0 | 1 | 截止 | 导通 | 导通 | 截止 | 0 |
| 1 | 0 | 导通 | 截止 | 截止 | 导通 | 0 |
| 1 | 1 | 导通 | 导通 | 截止 | 截止 | 0 |

因此逻辑表达式为：
$$
Y = \overline{A+B}
$$

这就是或非门。

将或非门的输出连接到非门的输入，就得到了或门。

### 例：异或门的搭建与优化

开头那个图就是异或门的一种搭建方式，需要22个晶体管。

思考后发现，异或门可以用与门和或非门来搭建。

逻辑表达式为：
$$
Y = A\oplus B = \overline{AB + \overline{A+B}}
$$

因此可以搭建如下电路：

![](./xor2.png)

该门电路需要14个晶体管。

进一步考虑异或门的全定制电路：
![](./xor3.png)

这个电路比之前的或非门稍微复杂一些，它结合了 **CMOS 反相器**和**传输门 (Transmission Gate)**。其核心功能是一个 **二输入异或门 (XOR Gate)**。

我们可以将电路拆解为三个主要部分进行分析：

#### 1. 电路组成部分

* **左侧反相器 (P1, N1)：** 输入为 **A**。其输出为 $\overline{A}$。
* **中间反相器 (P2, N2)：** 输入为 **B**。注意它的供电端：其上拉 PMOS (P2) 连接到输入 **A**，而下拉 NMOS (N2) 连接到$\overline{A}$。这实际上是一个**受控反相结构**。
* **右侧传输门 (P3, N3)：** 输入端连接到 **B**，输出端连接到 **Y**。
* N3 的控制极（栅极）连接到 $\overline{A}$（左侧反相器的输出）。
* P3 的控制极（栅极）连接到 **A**。



---

#### 2. 逻辑状态推导

我们根据输入 **A** 的状态来划分两种情况：

情况 A：当 $A = 0$ 时

* **左侧反相器：** 输出 $\overline{A} = 1$。
* **中间反相器：** P2 的源极接 A（即接地），N2 的源极接 $\overline{A}$（电势高），因此无论 B 是什么，P2 和 N2 均截止。
* **右侧传输门：** 无论B是什么，总有一个晶体管导通，即**传输门导通**，将B连接到Y。
* **结论：** 输出 $Y$ 直接等于输入 $B$。即当 $A=0$ 时，$Y = B$。

情况 B：当 $A = 1$ 时

* **左侧反相器：** 输出 $\overline{A} = 0$。
* **右侧传输门：** 无论B是什么，两个晶体管均截止，即**传输门截止**，切断了B到Y的连接。
* **中间反相器：** P2 的源极接 A（即接 $V_{cc}$）。此时 P2 和 N2 构成了一个标准的以 B 为输入的反相器。
* **结论：** 输出 $Y$ 等于 $B$ 的反相信号。即当 $A=1$ 时，$Y = \overline{B}$。

---

#### 3. 真值表与结论

| A | B | Y |
| --- | --- | --- |
| 0 | 0 | **0** (跟随 B) |
| 0 | 1 | **1** (跟随 B) |
| 1 | 0 | **1** (B 的反相) |
| 1 | 1 | **0** (B 的反相) |

* **逻辑功能：** 该电路实现的是 **异或 (XOR)** 运算，表达式为 $Y = A \oplus B$。
* **设计特点：** 这种设计使用了 6 个晶体管，比标准的 12 管 CMOS 异或门更节省空间，常用于高速、低功耗的逻辑单元设计中。

### Ex2:

要求设计一个同或门，易知同或门就是异或门的反相输出，因此可以将异或门的输出连接到非门的输入，就得到了同或门。

考虑优化，同或门可以用与非门和或门来搭建，逻辑表达式为：
$$
Y = \overline{\overline{AB}(A+B)}
$$

因此可以搭建如下电路：
![](./xnor.png)

只需要14个晶体管。

同或门的全定制电路设计只需在异或门全定制电路的基础上，将逻辑稍加改动：
- 把A接到N2的源极，$\overline{A}$接到P2的源极。
- 把A接到N3的栅极，$\overline{A}$接到P3的栅极。

在logisim中实现如下：
![](./exercise2.png)

## 通过门电路搭建基本组合逻辑电路

### 译码器

2-4译码器的逻辑表达式为：
$$
Y_0 = \overline{A_0} \overline{A_1}
$$
$$
Y_1 = A_0 \overline{A_1}
$$
$$
Y_2 = \overline{A_0} A_1
$$
$$
Y_3 = A_0 A_1
$$

电路图略。

### logisim练习

将2-4译码器封装为子电路，并在logisim中使用该子电路搭建3-8译码器。

![](./decoder1.png)

根据逻辑表达式搭建支持十六进制数字的七段数码管译码器。

![](./expression1.png)

逻辑表达式已给出，电路略。

简单给出显示电路：
![](./exercise3.png)

### 编码器

4-2优先编码器的逻辑表达式为：
$$
Y_0 = \overline{A_3} \overline{A_2} A_1 + A_3

Y_1 = \overline{A_3} A_2 + A_3
$$

电路图略。

选做任务略。

### 多路选择器

1位2选1选择器可用一个1-2译码器和若干门电路搭建，电路图如下：
![](./mux1.png)
